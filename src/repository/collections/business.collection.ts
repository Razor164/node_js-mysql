// import { MONGO } from '../../services/mongo.service';
import { Tables } from '../../models/enums/tables';

export async function getBusinessInfo(id: string): Promise<any> {
    // return MONGO.collection(Collections.Business).find({});
}